{
  "data": {
    "contractNumber": 1,
    "dateSigned": "{{ contracts[6].dateModified }}",
    "items": [
      {
        "additionalClassifications": [
          {
            "description": "{{ contracts[6]['items'][0].additionalClassifications[0].description }}",
            "id": "{{ contracts[6]['items'][0].additionalClassifications[0].id }}",
            "scheme": "{{ contracts[6]['items'][0].additionalClassifications[0].scheme }}"
          }
        ],
        "classification": {
          "description": "{{ contracts[6]['items'][0].classification.description }}",
          "id": "{{ contracts[6]['items'][0].classification.id }}",
          "scheme": "{{ contracts[6]['items'][0].classification.scheme }}"
        },
        "deliveryAddress": {
          "countryName": "{{ contracts[6]['items'][0].deliveryAddress.countryName }}",
          "locality": "{{ contracts[6]['items'][0].deliveryAddress.locality }}",
          "postalCode": "{{ contracts[6]['items'][0].deliveryAddress.postalCode }}",
          "region": "{{ contracts[6]['items'][0].deliveryAddress.region }}",
          "streetAddress": "{{ contracts[6]['items'][0].deliveryAddress.streetAddress }}"
        },
        "deliveryDate": {
          "endDate": "{{ contracts[6]['items'][0].deliveryDate.endDate }}",
          "startDate": "{{ contracts[6]['items'][0].deliveryDate.startDate }}"
        },
        "description": "{{ contracts[6]['items'][0].description }}",
        "description_en": "{{ contracts[6]['items'][0].description_en }}",
        "id": "{{ contracts[6]['items'][0].id }}",
        "quantity": 2,
        "relatedBuyer": "{{ contracts[6]['items'][0].relatedBuyer }}",
        "relatedLot": "{{ contracts[6]['items'][0].relatedLot }}",
        "unit": {
          "code": "{{ contracts[6]['items'][0].unit.code }}",
          "name": "{{ contracts[6]['items'][0].unit.name }}",
          "value": {
            "amount": 50.0,
            "valueAddedTaxIncluded": false
          }
        }
      }
    ],
    "period": {
      "startDate": "{{ contracts[6].dateModified }}",
      "endDate": "{{ from_date_iso(contracts[6].dateModified, days=365) }}"
    },
    "status": "active",
    "value": {
      "amount": 100,
      "amountNet": 100,
      "valueAddedTaxIncluded": false
    }
  }
}