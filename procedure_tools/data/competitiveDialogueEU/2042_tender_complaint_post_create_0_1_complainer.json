{
  "data": {
    "title": "{{ fake.sentence(nb_words=5) }}",
    "description": "{{ fake.paragraph(nb_sentences=2) }}",
    "relatedObjection": "{{ tender_complaints[0].objections[0].id }}",
    "recipient": "aboveThresholdReviewers",
    "relatedPost": "{{ tender_complaints[0].posts[0].id }}"
  }
}
