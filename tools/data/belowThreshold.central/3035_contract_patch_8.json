{
  "data": {
    "contractNumber": 3,
    "dateSigned": "{{ contracts[8].dateModified }}",
    "items": [
      {
        "additionalClassifications": [
          {
            "description": "{{ contracts[8]['items'][0].additionalClassifications[0].description }}",
            "id": "{{ contracts[8]['items'][0].additionalClassifications[0].id }}",
            "scheme": "{{ contracts[8]['items'][0].additionalClassifications[0].scheme }}"
          }
        ],
        "classification": {
          "description": "{{ contracts[8]['items'][0].classification.description }}",
          "id": "{{ contracts[8]['items'][0].classification.id }}",
          "scheme": "{{ contracts[8]['items'][0].classification.scheme }}"
        },
        "deliveryAddress": {
          "countryName": "{{ contracts[8]['items'][0].deliveryAddress.countryName }}",
          "locality": "{{ contracts[8]['items'][0].deliveryAddress.locality }}",
          "postalCode": "{{ contracts[8]['items'][0].deliveryAddress.postalCode }}",
          "region": "{{ contracts[8]['items'][0].deliveryAddress.region }}",
          "streetAddress": "{{ contracts[8]['items'][0].deliveryAddress.streetAddress }}"
        },
        "deliveryDate": {
          "endDate": "{{ contracts[8]['items'][0].deliveryDate.endDate }}",
          "startDate": "{{ contracts[8]['items'][0].deliveryDate.startDate }}"
        },
        "description": "{{ contracts[8]['items'][0].description }}",
        "description_en": "{{ contracts[8]['items'][0].description_en }}",
        "id": "{{ contracts[8]['items'][0].id }}",
        "quantity": 3,
        "relatedBuyer": "{{ contracts[8]['items'][0].relatedBuyer }}",
        "relatedLot": "{{ contracts[8]['items'][0].relatedLot }}",
        "unit": {
          "code": "{{ contracts[8]['items'][0].unit.code }}",
          "name": "{{ contracts[8]['items'][0].unit.name }}",
          "value": {
            "amount": 33.333334,
            "valueAddedTaxIncluded": false
          }
        }
      }
    ],
    "period": {
      "startDate": "{{ contracts[8].dateModified }}",
      "endDate": "{{ from_date_iso(contracts[8].dateModified, days=365) }}"
    },
    "status": "active",
    "value": {
      "amount": 100,
      "amountNet": 100,
      "valueAddedTaxIncluded": false
    }
  }
}